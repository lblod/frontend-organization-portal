{{#let
  (concat
    @model.changeEvent.type.label " " (@model.changeEvent.changeDate)
  )
  as |title|
}}
  {{page-title title}}
  {{breadcrumb
    title
    route="administrative-units.administrative-unit.change-events.details"
    models=(array @model.administrativeUnit.id @model.changeEvent.id)
  }}
{{/let}}

{{outlet}}