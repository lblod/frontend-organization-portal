<div class="au-c-body-container au-c-body-container--scroll">
  <div class="au-o-box au-o-flow au-o-flow--large">
    <PageHeader>
      <:title>Kerngegevens</:title>
      <:subtitle>{{@model.administrativeUnit.unit.name}}</:subtitle>
      <:action>
        <AuLink
          @route="administrative-units.administrative-unit.core-data.edit"
          @skin="button-secondary"
          @icon="pencil"
          @iconAlignment="left"
        >
          Bewerk
        </AuLink>
      </:action>
    </PageHeader>

    <DataCard>
      <:title>Bestuurseenheid</:title>
      <:card as |Card|>
        <Card.Columns>
          <:left as |Item|>
            <Item>
              <:label>Naam</:label>
              <:content>{{@model.administrativeUnit.unit.name}}</:content>
            </Item>
            <Item>
              <:label>Type bestuur</:label>
              <:content>
                {{@model.administrativeUnit.unit.classification.label}}
              </:content>
            </Item>
            <Item>
              <:label>Soort eredienst</:label>
              <:content
              >{{@model.administrativeUnit.unit.recognizedWorshipType.label}}</:content>
            </Item>
            {{#if @model.administrativeUnit.unit.denomination}}
              <Item>
                <:label>Strekking</:label>
                <:content>{{@model.administrativeUnit.unit.denomination}}</:content>
              </Item>
            {{/if}}
            {{#if
              (eq
                @model.administrativeUnit.unit.constructor.modelName
                "worship-service"
              )
            }}
              <Item>
                <:label>Grensoverschrijdend</:label>
                <:content>
                  {{@model.administrativeUnit.unit.crossBorderNominal}}
                </:content>
              </Item>
            {{/if}}
            {{!-- Temporarily disabled - cf https://binnenland.atlassian.net/browse/OP-583
            {{#if @model.administrativeUnit.unit.scope.label}}
              <Item>
                <:label>Werkingsgebied</:label>
                <:content>{{@model.administrativeUnit.unit.scope.label}}</:content>
              </Item>
            {{/if}} --}}
          </:left>
          <:right as |Item|>
            <Item>
              <:label>Status</:label>
              <:content>
                <OrganizationStatus
                  @status={{@model.administrativeUnit.unit.organizationStatus}}
                />
              </:content>
            </Item>
            {{! TODO: Take the last date from the change events }}
            {{#if @model.administrativeUnit.unit.resultedFrom}}
              <Item>
                <:content>
                  <AuHelpText @skin="tertiary" class="au-u-margin-top-none">
                    Gewijzigd op
                    {{date-format
                      @model.administrativeUnit.unit.resultedFrom.firstObject.date
                    }}
                  </AuHelpText>
                </:content>
              </Item>
            {{/if}}
            {{#each @model.administrativeUnit.unit.identifiers as |identifier|}}
              {{#if identifier.structuredIdentifier.localId}}
                <Item>
                  <:label>{{identifier.idName}}</:label>
                  <:content>
                    {{#let
                      identifier.structuredIdentifier.localId
                      as |localId|
                    }}
                      {{#if (this.isSharePointIdentifier identifier)}}
                        <AuLinkExternal
                          href="{{this.sharePointLinkBase}}{{localId}}"
                        >
                          {{localId}}
                          (externe link)
                        </AuLinkExternal>
                      {{else}}
                        {{localId}}
                      {{/if}}
                    {{/let}}
                  </:content>
                </Item>
              {{/if}}
            {{/each}}
          </:right>
        </Card.Columns>
      </:card>
    </DataCard>

    <DataCard>
      <:title>
        Primaire contactgegevens
      </:title>
      <:card as |Card|>
        <Card.Columns>
          <:left as |Item|>
            <Item>
              <:label>Adres</:label>
              <:content>
                {{@model.administrativeUnit.unit.primarySite.address.fullAddress}}
              </:content>
            </Item>

            <Item>
              <:label>Provincie</:label>
              <:content>
                {{@model.administrativeUnit.unit.primarySite.address.province}}
              </:content>
            </Item>
          </:left>
          <:right as |Item|>
            {{#if
              @model.administrativeUnit.unit.primarySite.contacts.firstObject.telephone
            }}
              <Item>
                <:label>Telefoonnummer</:label>
                <:content>
                  {{@model.administrativeUnit.unit.primarySite.contacts.firstObject.telephone}}
                </:content>
              </Item>
            {{/if}}
            {{#if
              @model.administrativeUnit.unit.primarySite.contacts.firstObject.website
            }}
              <Item>
                <:label>Website</:label>
                <:content>
                  <a
                    href={{@model.administrativeUnit.unit.primarySite.contacts.firstObject.website}}
                    class="au-c-link"
                    target="_blank"
                    rel="noopener noreferrer"
                  >{{@model.administrativeUnit.unit.primarySite.contacts.firstObject.website}}
                    (externe link)</a>
                </:content>
              </Item>
            {{/if}}
          </:right>
        </Card.Columns>
      </:card>
    </DataCard>

    {{#if
      (or
        @model.administrativeUnit.unit.isSubOrganizationOf.name
        @model.administrativeUnit.unit.isAssociatedWith.name
      )
    }}
      <DataCard>
        <:title>
          Gerelateerde organisaties
        </:title>
        <:card as |Card|>
          <Card.Columns>
            <:left as |Item|>
              {{#if @model.administrativeUnit.unit.isSubOrganizationOf.name}}
                <Item>
                  <:label>Centraal bestuur</:label>
                  <:content>
                    <AuLink
                      @route="administrative-units.administrative-unit"
                      @model={{@model.administrativeUnit.unit.isSubOrganizationOf.id}}
                    >
                      {{@model.administrativeUnit.unit.isSubOrganizationOf.name}}
                    </AuLink>
                  </:content>
                </Item>
              {{/if}}
              {{#if @model.administrativeUnit.subOrganizations}}
                {{#each
                  @model.administrativeUnit.subOrganizations
                  as |worship|
                }}
                  <Item>
                    <:label>Bestuur van de eredienst</:label>
                    <:content>
                      <AuLink
                        @route="administrative-units.administrative-unit"
                        @model={{worship.id}}
                      >
                        {{worship.name}}
                      </AuLink>
                    </:content>
                  </Item>
                {{/each}}
              {{/if}}
              {{#if @model.administrativeUnit.unit.isAssociatedWith.name}}
                <Item>
                  <:label>Representatief orgaan</:label>
                  <:content>
                    <AuLink
                      @route="organizations.organization"
                      @model={{@model.administrativeUnit.unit.isAssociatedWith.id}}
                    >
                      {{@model.administrativeUnit.unit.isAssociatedWith.name}}
                    </AuLink>
                  </:content>
                </Item>
              {{/if}}
            </:left>
          </Card.Columns>
        </:card>
      </DataCard>
    {{/if}}
  </div>
</div>