{{page-title @model.associatedStructure.name}}

{{breadcrumb
  @model.associatedStructure.name
  route="administrative-units.administrative-unit.legal-structures"
  models=(array @model.administrativeUnit.id @model.associatedStructure.id)
}}

{{outlet}}