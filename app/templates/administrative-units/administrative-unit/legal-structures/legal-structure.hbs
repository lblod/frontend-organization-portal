{{page-title @model.associatedStructure.name}}

{{breadcrumb
  this.model.associatedStructure.name
  route="administrative-units.administrative-unit.legal-structures"
  models=(array this.model.administrativeUnit.id this.model.associatedStructure.id)
}}

{{outlet}}