<PowerSelect
  @allowClear={{@allowClear}}
  @loadingMessage="Aan het laden..."
  @noMatchesMessage="Geen resultaten"
  @placeholder="Zoek type relatie"
  @options={{this.loadMembershipRolesTask.last}}
  @selected={{@selected}}
  @onChange={{@onChange}}
  @triggerId={{@id}}
  ...attributes
  as |role|
>
  {{role.opLabel}}
</PowerSelect>