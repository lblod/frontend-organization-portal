<PowerSelect
  @allowClear={{true}}
  @loadingMessage="Aan het laden..."
  @noMatchesMessage="Geen resultaten"
  @searchMessage="Typ om te zoeken"
  @searchField="label"
  @options={{this.fetchOrganizationStatusTask.last}}
  @selected={{@selected}}
  @onChange={{@onChange}}
  @triggerId={{@id}}
  as |organizationStatus|
>
  {{organizationStatus.label}}
</PowerSelect>