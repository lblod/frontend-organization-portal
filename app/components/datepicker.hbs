<AuInput
  @disabled={{@disabled}}
  @error={{@error}}
  @maskPlaceholder="DD-MM-YYYY"
  autocomplete="off"
  @mask="99-99-9999"
  @onChange={{this.onChange}}
  {{on 'focusout' this.focusOut}}
  @width={{this.width}}
  @id={{@id}}
  @value={{this.value}}
  ...attributes
/>