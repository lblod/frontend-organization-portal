<PowerSelect
  @allowClear={{true}}
  @loadingMessage="Aan het laden..."
  @noMatchesMessage="Geen resultaten"
  @searchEnabled={{true}}
  @searchMessage="Typ om te zoeken"
  @searchField="label"
  @options={{this.loadMandateRolesTask.last}}
  @selected={{@selected}}
  @onChange={{@onChange}}
  as |mandateRole|
>
  {{mandateRole.label}}
</PowerSelect>