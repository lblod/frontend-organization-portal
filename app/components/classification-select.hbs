<PowerSelect
  @allowClear={{true}}
  @loadingMessage="Aan het laden..."
  @noMatchesMessage="Geen resultaten"
  @searchEnabled={{true}}
  @searchMessage="Typ om te zoeken"
  @searchField="label"
  @options={{this.fetchClassificationTask.last}}
  @selected={{@selected}}
  @onChange={{@onChange}}
  as |classification|
>
  {{classification.label}}
</PowerSelect>