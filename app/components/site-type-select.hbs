<PowerSelect
  @allowClear={{true}}
  @loadingMessage="Aan het laden..."
  @options={{this.siteTypes}}
  @selected={{@selected}}
  @onChange={{@onChange}}
  @triggerId={{@id}}
  as |siteType|
>
  {{siteType.label}}
</PowerSelect>